# Build artifacts
build/*.dmg
build/temp.dmg
build/*.deb
# Hand-made backups of a build output. `build/*.dmg` does not match
# `onionpress-aug18.dmg.bak`, so a 167 MB artifact sat untracked and
# unignored, one `git add -A` away from being committed.
build/*.bak

# macOS
.DS_Store
*.swp
*.swo
*~

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
ENV/
env/
uv.lock

# User data
.onionpress/

# IDE
.vscode/
.idea/
*.iml

# Logs
*.log

# Build artifacts
build/onionpress.dmg
build/.cache/
py2app_build/
py2app_dist/

# Assembled .app bundle (built from app/ source directory)
OnionPress.app/

# Firefox extension testing copy
extension-firefox/

# Stress test results (contain secret keys)
onionheaven-stress-results/
tests/onionheaven-stress-results/

# Per-user Claude Code config (local permissions, hooks, etc.)
.claude/

# Stray build-host symlink guard (accidentally committed once from the fork-image workflow)
target
